# 看板前端发布与验证规范

> 起源：2026-09-18 生产事故（语法错误直接上线致全站图表崩溃 + 回滚后缓存版本号未同步致误判）。
> 本规范是推送代码到 `main`（触发 Vercel 自动部署）前的**强制流程**。

## 一、推送前门禁（强制，不可跳过）

每次修改 `js/`、`data/*.json` 后，推送前必须运行：

```bash
bash scripts/pre_push_check.sh
```

门禁内容：
1. **JS 语法检查**：对 `js/*.js` 逐个 `node --check`，任何解析错误立即拦截（10 秒即可拦住 9/18 那类圆括号不平衡事故）。
2. **JSON 有效性**：`data/*.json` 必须全部可解析。
3. **history 存活检查**：8 个已验证真实采集指标的 history 不得为空（防止 pipeline 清空复发；有意删除除外）。

退出码非 0 时**禁止 `git push`**。

## 二、改动分批，禁止打包上线

- 多个相互独立的修复**分开提交、分开推送、分开验收**。
- 一个 commit 只解决一类问题。这样某个修复出问题时，回滚只影响它本身，不会连坐其他正确修复（9/18 把 6 项打成 8 个 commit 一次上线，事故后只能整体撤销，正确修复被连坐）。
- 推送节奏：每批推送后、确认线上无 Console 红色错误，再推下一批。

## 三、缓存版本号管理（`index.html` 中 `?v=`）

格式：`?v=YYYYMMDD` + 当日字母序号（`a`/`b`/`c`…），例如 `20260919a`。

规则：
1. **任何对 `js/*.js` 的改动，必须同步 bump 该文件在 `index.html` 中的版本号**，否则用户浏览器继续加载旧缓存。
2. 同一天多次发布，字母递增：`a → b → c`；跨天重置为当天日期 + `a`。
3. **回滚后必须检查版本号**：若回滚的是 JS 内容，版本号也要同步处理（前进到一个新版本号，而不是退回旧号——旧号可能已被用户缓存）。9/18 回滚后版本号停留在 `20260916i`，浏览器不刷新，导致"回滚了依然不行"的误判。
4. 所有 JS 文件版本号可统一，也可逐个 bump，但**被改动的文件必须 bump**。

## 四、回滚

```bash
bash scripts/rollback.sh --dry <commit>   # 先预演
bash scripts/rollback.sh <commit>         # 安全 revert（保留历史）
bash scripts/rollback.sh --hard <commit>  # 仅在连续多个坏 commit 时硬重置
```

回滚后立即按第三条处理版本号，并跑一遍门禁。

## 五、修复前先验证假设

- 对"数据导致渲染崩溃"类问题，**先取到 Console 真实错误 + 核对运行时数据结构，再改代码**。
- 不允许基于未验证的假设推送修复（9/18 的 `_cleanHist()` 在 CIO 已指出"JSON 无 null 点"后仍推送，被用户实测证伪，浪费一轮发布）。
- 修复后要构造边界输入做攻击测试：空数组、字符串数字、缺字段、含单位符号的值、null 点。

## 六、验收顺序（与 CIO SOP 对齐）

1. **生死线先行**：页面整体可加载、Console 无红色错误、JS 可解析。
2. 再做逐字段、逐数据点、图表格式的细节核对。
3. 云端无法访问 Vercel 时，浏览器实测由用户完成；但语法/数据层门禁必须在推送前由开发方自己通过，不得把可在本地拦住的错误转嫁给用户。
